version: '3.8'

services:
  backroom:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backroom_app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
